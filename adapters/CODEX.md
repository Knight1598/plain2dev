# Codex

Install to `.agents/skills/plain2dev/`. Invoke `$plain2dev` or request it by name. Preserve project instructions and permissions. Spec-only requests do not authorize application edits. Memory stays at the target root under `.plain2dev/`.

[Official OpenAI documentation](https://learn.chatgpt.com/docs/build-skills). Verify discovery in the installed client; restart the session if necessary. The installer checks placement, not an active Codex session.
