# Claude Code

Install to `.claude/skills/plain2dev/`. Invoke `/plain2dev` followed by the request. No provider-specific tool permission override is used. Preserve CLAUDE.md and project conventions.

[Official Claude Code documentation](https://code.claude.com/docs/en/skills). Runtime behavior depends on the client/model; the installer does not launch Claude Code.
