# เริ่มใช้ Plain2Dev

1. แตก ZIP แล้วเปิดโปรเจกต์ที่ต้องการทำงานใน Codex หรือ Claude Code
2. บอก AI ว่าโฟลเดอร์ Plain2Dev อยู่ที่ไหน แล้วส่งข้อความนี้:

> อ่าน INSTALL.md ในแพ็กเกจ Plain2Dev แล้วติดตั้งให้โปรเจกต์นี้ ใช้ตัวเลือกที่ตรงกับ AI ที่ฉันใช้อยู่ ตรวจการติดตั้งและบอกผล จากนั้นช่วยแปลงคำสั่งของฉันเป็นข้อกำหนดก่อนเริ่มเขียนโค้ด

3. เมื่อ AI ตรวจไฟล์ผ่านแล้ว ลองสั่ง:

> ใช้ Plain2Dev ช่วยแปลงคำสั่งนี้: อยากให้หัวหน้าใส่เหตุผลตอนปฏิเสธการเบิกของ

AI จะสรุปสิ่งที่เข้าใจและถามเฉพาะที่จำเป็น คุณแก้ความเข้าใจได้ทุกครั้ง

Codex เรียกด้วย `$plain2dev` ส่วน Claude Code ใช้ `/plain2dev` ถ้ายังไม่เห็นให้เปิด session ใหม่ AI อื่นให้อ่าน `.plain2dev/engine/SKILL.md` โดยตรง

ไม่ต้องใช้ GitHub หรือบัญชีใหม่ในการติดตั้ง ต้องมี Node.js 22+ สำหรับตัวติดตั้ง หากไม่มี ให้ AI คัดลอกตาม INSTALL.md
