{
  "name": "plain2dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Plain-language requirements and project context skill package",
  "engines": { "node": ">=22" },
  "scripts": { "test": "node --test", "check": "node scripts/validate.mjs" }
}
